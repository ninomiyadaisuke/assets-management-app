export type Result = {
  stockId: string;
  currentStockPrice: number;
  dividend: number;
};

export type Stock = {
  stockId: string;
  stockName: string;
  currentStockPrice: number;
  dividend: number;
};
